#include<stdio.h>
#include<conio.h>
 main()
 {
 	int a,b,c;
 	printf("enter the values of a,b,c");
    scanf("%d%d%d",&a,&b,&c);
    if(a<b)
    if(a<c)
	{
	 printf("%d is lowest",a);	
	}	
	else
	{
		printf("%d is lowest",c);
	}
	else
	{
		if(b<c)
		{
			printf("%d is lowest",b);
		}
		else
		{
			printf("%d is lowest",c);
		}
	}
	getch();
 }
 
